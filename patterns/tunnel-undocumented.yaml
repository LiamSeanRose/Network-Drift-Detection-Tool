name: "Undocumented tunnel present on the device"
object_type: tunnel
field: _tunnel
drift_kinds:
  - missing_in_intent
vendors: []
cause: "The device runs an overlay tunnel NetBox does not model. Either a legitimate tunnel missing from the source of truth, or one configured directly on the device and never documented."
fix: "Add the tunnel to NetBox if it belongs, or remove it from the device if it is not authorized. Resolve on the intent side."
