apiVersion: v2
name: netdrift
description: Network Drift Detection Tool — open-source network drift detection.
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: postgresql
    version: "^16.0.0"
    repository: "https://charts.bitnami.com/bitnami"
